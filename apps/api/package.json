{
  "name": "@valet/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file ../../env/.env --hot src/index.ts",
    "start": "bun --env-file ../../env/.env run src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint yet'"
  },
  "dependencies": {
    "@valet/db": "workspace:*",
    "@valet/env": "workspace:*",
    "hono": "^4.6.12",
    "openai": "^4.70.2",
    "serpapi": "^2.2.1",
    "zod": "^3.23.8"
  }
}
